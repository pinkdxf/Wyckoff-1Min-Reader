name: Daily Stock AI Report

on:
  workflow_dispatch:      # 允许手动触发
  schedule:
    - cron: "0 4 * * *"   # UTC 04:00 = 北京时间 12:00 (午盘)
    - cron: "15 7 * * *"  # UTC 07:15 = 北京时间 15:15 (收盘)

permissions:
  contents: write

concurrency:
  group: stock-ai-report-main
  cancel-in-progress: true

jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      # === 1. 安装系统依赖 (中文字体 + PDF底层库) ===
      # 必须安装 libcairo2-dev 否则 xhtml2pdf 无法安装
      # 必须安装 fonts-wqy-microhei 否则 PDF 中文乱码
      - name: Install System Dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y fonts-wqy-microhei ttf-wqy-microhei libcairo2-dev pkg-config python3-dev
          fc-cache -f -v

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      # === 2. 安装 Python 依赖 ===
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install pandas akshare mplfinance openai requests markdown xhtml2pdf
          if [ -f requirements.txt ]; then pip install -r requirements.txt; fi

      # === 3. 运行分析脚本 (自动读取 stock_list.txt) ===
      - name: Run Analysis Script
        env:
          # Google Gemini 配置
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          GEMINI_MODEL: "gemini-3-flash-preview"
          # OpenAI 配置
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
          #AI_MODEL: "gpt-4o"
          # 通用配置
          WYCKOFF_PROMPT_TEMPLATE: ${{ secrets.WYCKOFF_PROMPT_TEMPLATE }}
          SYMBOLS: "600970" # 如果 stock_list.txt 不存在，以此为默认值
          BARS_COUNT: "600"
        run: |
          mkdir -p data reports
          python main.py

      # === 4. 清理 7 天前的旧数据 ===
      - name: Cleanup old data
        run: |
          echo "Cleaning up files older than 7 days..."
          find reports/ -name "*.*" -type f -mtime +7 -print -delete
          find data/ -name "*.csv" -type f -mtime +7 -print -delete

      # === 5. 提交新生成的 PDF 和 CSV 到仓库 ===
      - name: Commit and push changes
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add --all
          if git diff --staged --quiet; then
            echo "No changes to commit"
          else
            git commit -m "chore: daily report $(date +'%Y-%m-%d')"
            git pull --rebase origin main
            git push origin HEAD:main
          fi

   # === 6. 精准推送本次生成的报告 ===
      - name: Notify Telegram (Push List)
        if: success()
        env:
          TG_BOT_TOKEN: ${{ secrets.TG_BOT_TOKEN }}
          TG_CHAT_ID: ${{ secrets.TG_CHAT_ID }}
        shell: bash
        run: |
          set -euo pipefail
          
          if [ ! -f push_list.txt ]; then
            echo "::warning::没有找到 push_list.txt，本次运行没有新报告产生。"
            exit 0
          fi
          
          echo "=== 开始推送 push_list.txt 中的文件 ==="
          
          # 逐行读取 push_list.txt
          while IFS= read -r PDF_FILE || [ -n "$PDF_FILE" ]; do
            # 去除可能的空白符
            PDF_FILE=$(echo "$PDF_FILE" | xargs)
            
            if [ -f "$PDF_FILE" ]; then
              echo "----------------------------------------"
              echo "🚀 Sending: $PDF_FILE"
              
              FILENAME=$(basename "$PDF_FILE")
              
              curl -s -X POST "https://api.telegram.org/bot${TG_BOT_TOKEN}/sendDocument" \
                -F "chat_id=${TG_CHAT_ID}" \
                -F "document=@$PDF_FILE" \
                -F "caption=📊 Analysis: $FILENAME"
                
              echo "✅ Sent."
              sleep 3
            else
              echo "::error::文件不存在: $PDF_FILE"
            fi
          done < push_list.txt
          
          echo "=== 推送完毕 ==="
